# Held drafts — NOT deployed

Anything in `public/` is deployed and publicly reachable, including paths starting with
an underscore. `public/news/_drafts/` was not a private directory: the Sineidocracia
partnership post sat there on HOLD and was serving a 200 to anyone with the URL.

Drafts live here instead, outside `public/`, where the deploy cannot reach them.
Move a post into `public/news/` only when it is cleared to publish.

- `sineidocracia-partnership.html` — held pending Felipe's reply.
