<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" aria-labelledby="dissensusTitle">
  <title id="dissensusTitle">Dissensus AI Logo</title>
  <style>
    .dissensus-container {
      transform-origin: 50px 50px;
      animation: orbit 15s linear infinite;
    }
    
    .dissensus-shape {
      fill: #ffffff; /* Adjust color as needed, white for dark bg */
      transform-origin: 50px 50px;
      opacity: 0.95;
    }

    .shape-yang {
      animation: pacing-A 5s ease-in-out infinite alternate;
    }

    .shape-yin {
      animation: pacing-B 5s ease-in-out infinite alternate;
    }

    @keyframes orbit {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    /* Asymmetric pacing: One speeds up while the other slows down, 
       creating a gap that opens and closes, but they never touch. */
    @keyframes pacing-A {
      0% { transform: rotate(0deg) scale(0.96); }
      50% { transform: rotate(10deg) scale(1.0); }
      100% { transform: rotate(0deg) scale(0.96); }
    }

    @keyframes pacing-B {
      0% { transform: rotate(0deg) scale(0.96); }
      50% { transform: rotate(-5deg) scale(0.92); } /* Different scale/rotation to be asymmetric */
      100% { transform: rotate(0deg) scale(0.96); }
    }
  </style>

  <g class="dissensus-container">
    <!-- Shape 1: Top-Right "Yang" style comma -->
    <path class="dissensus-shape shape-yang" d="M 50 10 A 40 40 0 0 1 50 90 A 20 20 0 0 1 50 50 A 20 20 0 0 0 50 10 Z" />
    
    <!-- Shape 2: Bottom-Left "Yin" style comma (Rotated 180 visually by coordinates) -->
    <!-- M 50 90 start, outer arc to 50 10 left, inner arc to 50 50, inner arc to 50 90 -->
    <path class="dissensus-shape shape-yin" d="M 50 90 A 40 40 0 0 1 50 10 A 20 20 0 0 1 50 50 A 20 20 0 0 0 50 90 Z" />
  </g>
</svg>